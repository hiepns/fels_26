<% provide(:title, 'Create new word') %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="box box-primary">
      <div class="box-header">
        <h3 class="box-title">Create new word</h3>
      </div>
      <%= form_for [:admin, @word], url: admin_wordlists_path do |f| %>
        <div class="box-body">
          <div class="form-group">
            <%= f.label :category_id %>
            <%= f.select :category_id, options_for_select(@categories), class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :word %>
            <%= f.text_field :word, class: 'form-control' %>
          </div>
            <%= f.fields_for :answers do |ans| %>
              <div class="form-group">
                <%= ans.label :answer %>
                <%= ans.text_field :answer %>
                <%= ans.label :correct %>
                <%= ans.check_box :correct %>
              </div>
            <% end %>
          <div class="form-group">
            <%= f.submit "Create", class: "btn btn-primary" %>
            <%= f.submit "Clear", type: "reset", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
</div>